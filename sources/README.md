# Source evidence snapshots

这些文件是 CBS 图页面需要展示的只读源码快照，不是新的实现分支：

- `current/`：来自 `gambit` 的 `ColliderBit_solo_development`，当前快照为 `65aca0890d`。
- `prototype/`：来自 `/Users/p.zhu/Gambit-Workshop/worktree/SUSYRun2` 的 `private-SUSYRun2`，原型快照为 `5989e2d27a`。

它们让 GitHub Pages 中的源码证据链接保持自包含。真正的实现和完整历史仍以 Gambit 仓库为准。
